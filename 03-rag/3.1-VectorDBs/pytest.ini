[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --tb=short -q
markers =
    integration: requires Qdrant running on localhost:6333
    slow: takes more than 10 seconds
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
