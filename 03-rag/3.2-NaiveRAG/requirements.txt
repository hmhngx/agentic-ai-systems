# Zero LangChain. Zero LlamaIndex. Raw SDK only.
# Requires: Qdrant running on localhost:6333
# Run: docker run -d -p 6333:6333 qdrant/qdrant:latest
# Set: OPENROUTER_API_KEY in .env
# tiktoken: token counting for chunk size validation (offline, no network needed)
#   If tiktoken BPE download fails (network restricted), falls back to len//4
openai>=1.40.0
qdrant-client>=1.9.0
pymupdf>=1.24.0
tiktoken>=0.7.0
python-dotenv>=1.0.0
numpy>=1.26.0
