[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -q
markers =
    slow: semantic chunker and embedding model tests (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning:transformers
