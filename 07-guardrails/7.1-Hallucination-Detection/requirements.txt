# Day-10 hallucination detector. Pinned for reproducibility.
# The scoring core needs only numpy (+ stdlib difflib); the rest support the
# optional LLM judge, the live naive-RAG bridge, and .env loading.
numpy==2.1.3
openai>=1.55.3          # OpenRouter is OpenAI-compatible; needs 1.55.3+ for httpx 0.28
qdrant-client==1.12.1   # only used by the live --pdf naive-RAG bridge
python-dotenv==1.0.1
pytest==8.3.3
